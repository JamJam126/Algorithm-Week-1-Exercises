#include <iostream>

const std::string USERNAME = "admin";
const std::string PASSWORD = "password";

int main () {
/**
 * Create a basic login system to check if the user entered 
 * the correct username and password
 * 
 * Tips:
 * - Use string to store username and password
 * - User if-else and == operator to compare
 * 
 * Predefined:
 * - USERNAME = "admin"
 * - PASSWORD = "1234"
 *
 * Expected Output
 * - Login Successfully! (if correct)
 * - Login Failed! (if incorrect)
 */

    return 0;
}